# Copy to .env.dev (or .env) and fill in. Never commit real values.
# Used by: ui (Streamlit), docker-compose, optional API posters.

APP_ENV=dev
TMDB_API_KEY=your_tmdb_api_key_here

# Optional (e.g. Google Drive IDs for data)
# MOVIES_DIC_ID=
# TAG_SIMILARITY_ID=
# MOVIES_ID=
